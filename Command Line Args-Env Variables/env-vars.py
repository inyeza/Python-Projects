import os

print(os.getenv("password"))

# before executing the code on the command line we need to set first the env variables:
# Unix-based systems
#export API_KEY='your_api_key'

# Windows
#set API_KEY='your_api_key'

# run the code using: python env-vars.py

